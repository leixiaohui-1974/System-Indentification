# 水力数字孪生系统完整说明文档

## 项目概述

这是一个基于Python开发的**水力数字孪生仿真框架**，专门用于模拟和研究"一闸两渠池"水力系统的数字孪生技术。该系统实现了完整的同步数字孪生概念，包括高保真仿真、故障诊断、参数辨识和自适应控制。

### 核心概念

**数字孪生架构**：
- **Model A（高保真模型）**：基于有限体积法求解一维圣维南方程的精确物理仿真，代表"真实世界"
- **Model B（轻量化孪生模型）**：采用积分时滞模型的简化数字孪生，用于实时预测和控制
- **在线诊断与同步**：通过故障检测和系统辨识实现模型参数的自适应更新

## 项目结构

```
System-Indentification/
├── digital_twin_hydraulic_system/          # 核心仿真框架
│   ├── models/                            # 物理模型模块
│   │   ├── fvm_channel_model.py          # Model A: 高保真FVM模型
│   │   ├── id_channel_model.py           # Model B: 简化数字孪生模型
│   │   └── gate_model.py                 # 闸门水力模型
│   ├── diagnostics/                      # 诊断与辨识模块
│   │   ├── fault_detector.py             # 传感器故障检测
│   │   ├── system_identifier.py          # 参数辨识算法(RLS/EKF/UKF)
│   │   └── data_preprocessor.py          # 数据预处理
│   ├── main.py                           # 主程序入口
│   ├── config.py                         # 全局配置参数
│   ├── simulation_manager.py             # 仿真流程管理器
│   ├── sensor_simulator.py               # 传感器仿真器
│   └── visualization.py                  # 结果可视化
├── tests/                                # 单元测试
├── app.py                                # Streamlit交互式Web应用
├── offline_identifier.py                 # 离线参数辨识
└── pyproject.toml                        # 项目配置
```

## 核心功能模块

### 1. 仿真管理器 (`simulation_manager.py`)

**功能**：整个框架的核心控制器，负责协调所有组件的运行
- 管理双模型并行仿真（Model A和Model B）
- 控制数据流：传感器读取 → 数据清洗 → 故障诊断 → 参数辨识 → 模型更新
- 处理扰动事件的注入和管理
- 集成扩展卡尔曼滤波器进行状态和参数估计

**核心工作流程**：
1. 步进"真实世界"模型（Model A）
2. 模拟传感器读数（带噪声）
3. 数据预处理和清洗
4. 故障诊断和数据隔离
5. 参数辨识和模型更新
6. 步进数字孪生模型（Model B）
7. 记录和可视化结果

### 2. 物理模型模块 (`models/`)

#### FVM渠道模型 (`fvm_channel_model.py`)
**功能**：高保真"真实世界"仿真器
- 基于有限体积法求解一维圣维南方程组
- 采用HLL黎曼求解器处理激波和稀疏波
- 支持复杂初始条件（如溃坝问题）
- 自适应时间步长控制（CFL条件）

**数学模型**：
```
∂A/∂t + ∂Q/∂x = 0                    (连续性方程)
∂Q/∂t + ∂(Q²/A)/∂x + gA∂h/∂x = gA(S₀ - Sf)  (动量方程)
```

#### ID渠道模型 (`id_channel_model.py`) 
**功能**：轻量化数字孪生模型
- 采用离散时间积分时滞模型
- 参数可在线自适应更新
- 计算效率高，适用于实时控制

**数学模型**：
```
h(k) = a₁ × h(k-1) + b₁ × q(k-d)
```

#### 闸门模型 (`gate_model.py`)
**功能**：水流通过闸门的水力计算
```
Q = Cq × a × b × √(2gh)
```

### 3. 诊断与辨识模块 (`diagnostics/`)

#### 故障检测器 (`fault_detector.py`)
**功能**：多层次传感器故障诊断和隔离
- **冗余检查**：基于物理约束的交叉验证
- **噪声检测**：统计方法识别异常噪声水平
- **漂移检测**：基于模型预测偏差的长期趋势分析
- **故障隔离**：有状态的故障记忆和数据隔离

**支持的故障类型**：
- `stuck`：传感器卡死故障
- `increased_noise`：噪声异常增大
- `drift`：传感器缓慢漂移

#### 系统辨识器 (`system_identifier.py`)
**功能**：在线参数辨识和估计
- **递归最小二乘法（RLS）**：辨识Model B参数
- **扩展卡尔曼滤波（EKF）**：联合状态和参数估计
- **无迹卡尔曼滤波（UKF）**：处理强非线性系统

**算法特点**：
- 自适应遗忘因子
- 协方差矩阵自动调节
- 参数收敛性监控

### 4. 传感器仿真器 (`sensor_simulator.py`)

**功能**：模拟真实传感器的测量过程
- 高斯噪声注入
- 多种故障模式注入
- 时间相关的故障演化

**故障注入能力**：
- 卡死故障（固定值）
- 噪声故障（噪声水平变化）
- 漂移故障（缓慢偏移）

## 仿真场景

### 1. 传感器故障场景
**目的**：验证系统的故障检测和隔离能力
```python
def run_sensor_fault_scenario():
    sim_manager.add_perturbation(
        event_time=1500,
        event_type='sensor_fault',
        sensor_id='q_gate_down',
        fault_type='stuck',
        value=0.0
    )
```

### 2. 物理参数变化场景
**目的**：测试系统对物理世界变化的自适应能力
```python
def run_roughness_change_scenario():
    sim_manager.add_perturbation(
        event_time=1800,
        event_type='roughness_change',
        new_roughness=0.040  # 糙率增大
    )
```

### 3. 组合扰动场景
**目的**：验证系统处理多重连续扰动的能力
- 首先引入物理参数变化
- 然后在系统适应过程中引入传感器故障

### 4. 参数估计场景（EKF）
**目的**：验证扩展卡尔曼滤波器的参数估计能力
- 在真实模型和孪生模型之间引入参数差异
- 通过EKF在线估计真实参数

### 5. 数据生成场景
**目的**：为离线辨识生成丰富的训练数据
- 遍历多个操作点（流量×闸门开度）
- 生成稳态和动态响应数据

## 交互式Web界面 (`app.py`)

基于Streamlit开发的实时交互式界面：

**功能特性**：
- 实时仿真控制（启动/暂停）
- 参数动态调节（闸门开度、上游流量）
- 实时故障注入
- 动态图表更新
- 仿真日志显示

**界面布局**：
- 左侧控制面板：场景选择、参数调节、故障注入
- 右侧结果显示：状态对比图、参数收敛图、仿真日志

## 离线辨识工具 (`offline_identifier.py`)

**功能**：基于仿真数据的批量参数辨识
- 分段线性建模
- 批量最小二乘法
- 参数可视化分析
- 结果导出为JSON格式

**工作流程**：
1. 读取仿真生成的CSV数据
2. 按操作点分段
3. 对每个分段执行参数辨识
4. 可视化辨识结果
5. 保存参数到文件

## 配置管理 (`config.py`)

**集中式配置系统**：
- 仿真控制参数（时间步长、仿真时长）
- 物理参数（渠道几何、糙率系数）
- 噪声水平设置
- 算法超参数（遗忘因子、收敛阈值）

**参数分类**：
```python
# 仿真控制
TIMESTEP = 1.0
SIMULATION_DURATION = 3600

# 渠道几何
CHANNEL_LENGTH = 5000.0
CHANNEL_BOTTOM_WIDTH = 20.0
CHANNEL_SIDE_SLOPE = 1.5

# 物理参数
MANNING_ROUGHNESS_TRUE = 0.025
GATE_DISCHARGE_COEFFICIENT_TRUE = 0.61

# 算法参数
RLS_FORGETTING_FACTOR = 0.98
```

## 测试框架 (`tests/`)

**单元测试覆盖**：
- `test_fault_detector.py`：故障检测算法测试
- `test_gate_model.py`：闸门模型验证
- `test_system_identifier.py`：参数辨识算法测试

## 依赖和环境

**核心依赖**：
```toml
dependencies = [
    "numpy",        # 数值计算
    "pandas",       # 数据处理  
    "matplotlib",   # 图形绘制
    "streamlit",    # Web界面
]
```

## 使用指南

### 1. 基本仿真运行
```bash
# 运行默认场景
python -m digital_twin_hydraulic_system.main

# 启动Web界面
streamlit run app.py
```

### 2. 场景自定义
在`main.py`中选择或定义仿真场景：
```python
def main():
    # 选择要运行的场景
    run_sensor_fault_scenario()
    # run_roughness_change_scenario() 
    # run_combined_scenario()
```

### 3. 参数调节
修改`config.py`中的参数以适应不同的仿真需求。

### 4. 离线辨识
```bash
# 首先生成数据
python -m digital_twin_hydraulic_system.main  # 运行数据生成场景

# 然后执行离线辨识
python offline_identifier.py
```

## 技术特点

### 1. 模块化设计
- 清晰的组件分离和接口设计
- 易于扩展和维护
- 支持组件独立测试

### 2. 多算法集成
- 递归最小二乘法（RLS）
- 扩展卡尔曼滤波（EKF）
- 无迹卡尔曼滤波（UKF）
- HLL黎曼求解器

### 3. 实时交互能力
- Streamlit Web界面
- 动态参数调节
- 实时故障注入
- 在线可视化

### 4. 故障处理机制
- 多层次故障检测
- 自动数据隔离
- 系统状态记忆
- 鲁棒性保证

## 应用场景

1. **水力工程研究**：渠道和闸门系统的数字化仿真
2. **故障诊断研究**：传感器网络的可靠性分析
3. **控制算法开发**：自适应控制策略验证
4. **教学演示**：数字孪生概念的直观展示
5. **参数辨识研究**：在线和离线辨识算法比较

## 扩展方向

1. **更复杂的物理模型**：二维/三维流体力学模型
2. **机器学习集成**：神经网络参数辨识
3. **分布式仿真**：多节点并行计算
4. **实时硬件接口**：与真实传感器系统连接
5. **云端部署**：基于Web的大规模仿真服务

## 总结

这个水力数字孪生系统是一个功能完整、设计先进的仿真框架，成功实现了数字孪生的核心概念：高保真建模、实时同步、故障诊断和自适应更新。该系统具有良好的模块化结构、丰富的算法集成和直观的交互界面，为水力工程和数字孪生技术研究提供了强有力的工具。