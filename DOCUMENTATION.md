# 数字孪生仿真框架综合文档

## 第一部分：项目概述与架构

### 1. 项目目标与核心概念

本项目旨在创建一个全面的仿真框架，用于演示“一闸两渠池”水力系统的 **同步数字孪生 (Synchronized Digital Twin)** 概念。

其核心思想是：
- **高保真模型 (High-Fidelity Model)**：利用一个基于物理规律的、精确的仿真模型（模型A：有限体积法）来模拟“真实世界”的物理过程。此模型作为基准真相（Ground Truth）的来源。
- **轻量化孪生模型 (Lightweight Twin Model)**：并行运行一个计算上更简单、更快速的简化模型（模型B：积分时滞模型）。该模型旨在用于实时预测、控制与优化等实际业务。
- **在线诊断与同步**：系统的核心是一个在线诊断与辨识模块。它持续地接收来自“真实世界”的模拟传感器数据，并执行两大任务：
    1.  **故障诊断与溯源**：利用数据冗余和内置逻辑，诊断传感器是否发生故障（如漂移、卡死），或物理系统是否发生未预期的变化（如漏水、参数改变）。
    2.  **参数自适应**：在确认数据可靠后，启动在线系统辨识算法（如递归最小二乘法RLS），实时估计并更新轻量化孪生模型（模型B）的参数，使其输出能够紧密跟踪高保真模型（模型A）的输出。

通过这个闭环工作流，孪生模型（模型B）能够**自适应**物理世界（模型A）中发生的各种变化，从而实现“同步”，确保其对现实世界的表征始终是准确有效的。

### 2. 代码文件结构总览

项目代码遵循模块化的、面向对象的结构，以清晰地分离各个功能组件。

```
digital_twin_hydraulic_system/
├── main.py                     # 主入口，用于启动和管理仿真场景
├── config.py                   # 存放所有配置，如渠道几何、初始参数、噪声水平等
├── simulation_manager.py       # 仿真流程编排器，协调模型运行和数据流
├── sensor_simulator.py         # 模拟传感器读数，并添加噪声
├── visualization.py            # 结果可视化模块
├── models/                     # 存放物理模型
│   ├── gate_model.py           # 闸门模型 (流量计算)
│   ├── fvm_channel_model.py    # 模型A: 高保真“真实世界”模拟器
│   └── id_channel_model.py     # 模型B: 简化版“数字孪生”模型
└── diagnostics/                # 存放诊断与辨识算法
    ├── data_preprocessor.py    # 数据清洗模块
    ├── fault_detector.py       # 传感器故障诊断与隔离
    └── system_identifier.py    # 核心辨识算法 (RLS, EKF, UKF等)
```

**各文件功能简介:**

- **`main.py`**: 整个程序的起点。其主要职责是定义一个具体的仿真场景（例如，在特定时间引入一个传感器故障），初始化 `SimulationManager`，并启动仿真。
- **`config.py`**: 全局配置文件。将所有可调参数（如渠道尺寸、水力系数、仿真时长、噪声大小等）集中存放于此，便于管理和修改，避免硬编码。
- **`simulation_manager.py`**: **项目核心控制器**。它实例化所有模型和诊断模块，并在主循环中精确地编排数据流：从模型A生成数据 -> 传感器模拟 -> 数据清洗 -> 故障诊断 -> 参数辨识 -> 更新模型B。
- **`sensor_simulator.py`**: 模拟现实世界的传感器。它从高保真模型A中提取指定位置的“真值”，并根据配置添加高斯噪声，以生成模拟的传感器读数。它同样能够模拟多种传感器故障模式。
- **`visualization.py`**: 结果可视化工具。在仿真过程中收集关键数据，并在仿真结束后生成图表，如模型A与模型B的水位对比图、参数收敛过程图等。
- **`models/`**:
    - `gate_model.py`: 实现了闸门的过流能力计算，是连接上下游渠道的关键水力结构。
    - `fvm_channel_model.py`: **高保真模型A**。当前版本为一个占位符，但其设计目标是实现一个基于有限体积法的圣维南方程求解器，以精确模拟渠道内的非恒定流过程。
    - `id_channel_model.py`: **轻量化孪生模型B**。采用计算简单的积分时滞模型，其参数（如`a1`, `b1`）由`system_identifier`模块在线更新。
- **`diagnostics/`**:
    - `data_preprocessor.py`: 对原始、带噪声的传感器数据进行预处理（如滑动平均滤波），为后续的诊断和辨识提供更平滑的数据。
    - `fault_detector.py`: **故障诊断核心**。它是一个有状态的模块，利用数据间的冗余关系（例如，闸门公式计算流量 vs. 流量计实测流量）来判断传感器是否失效。一旦确认故障，它会记住该状态并持续隔离此传感器的数据。
    - `system_identifier.py`: **参数辨识核心**。封装了各种在线系统辨识算法。当前版本实现了递归最小二乘法（RLS），用于辨识模型B的参数。未来可扩展以包含用于辨识模型A参数的EKF/UKF等更高级算法。

---
## 第二部分：核心模块详解

本部分将深入介绍项目中每个关键模块的设计和功能。

### 1. `config.py` - 全局配置中心

此文件是整个仿真框架的“控制面板”。它将所有重要的物理参数、仿真参数和算法超参数集中在一个地方，实现了代码与配置的分离。

- **主要参数**:
    - `TIMESTEP`, `SIMULATION_DURATION`: 控制仿真的时间和步长。
    - `NOISE_LEVEL_*`: 定义了添加到模拟传感器读数中的高斯噪声的标准差，用于模拟真实世界传感器的不确定性。
    - `CHANNEL_*`: 定义了渠道的几何属性，如长度、坡度、底宽和边坡。
    - `FVM_MODEL_*`: 高保真模型A的参数，如空间离散化的单元数量和曼宁糙率系数的“真实值”。
    - `GATE_*`: 闸门模型的物理参数和流量系数的“真实值”。
    - `INITIAL_*`, `UPSTREAM_*`: 定义了整个系统仿真的初始状态和边界条件。
    - `RLS_*`, `KF_*`: 系统辨识算法所需的超参数，如RLS的遗忘因子等。

- **设计思想**:
    通过修改此文件，用户可以轻松地测试不同物理条件下或不同算法参数下的系统行为，而无需改动任何核心的逻辑代码。

### 2. `main.py` - 仿真场景定义与启动

这是运行仿真的入口脚本。它的核心功能是定义一个具体的“故事线”（即场景），然后启动仿真来执行这个故事。

- **核心功能**:
    1.  **场景定义**: 在`main`函数内部，通过调用`simulation_manager`实例的`add_perturbation`方法，可以精确地安排在仿真的哪个时间点发生何种类型的扰动。
    2.  **仿真启动**: 初始化`SimulationManager`并调用其`run_simulation()`方法。

- **示例 (`run_sensor_fault_scenario`)**:
    ```python
    def run_sensor_fault_scenario():
        """
        Defines and runs a scenario to test sensor fault detection.
        """
        print("\n--- Running Sensor Fault Scenario ---")
        # 1. 初始化仿真管理器
        sim_manager = SimulationManager(config)

        # 2. 调度一个扰动事件
        sim_manager.add_perturbation(
            event_time=1500,                # 在第1500秒
            event_type='sensor_fault',      # 事件类型：传感器故障
            sensor_id='q_gate_down',        # 故障传感器ID
            fault_type='stuck_at_zero'      # 故障模式：卡在0值
        )

        # 3. 运行仿真
        sim_manager.run_simulation()
    ```
    这个例子清晰地展示了如何构建一个测试用例：系统先正常运行1500秒，然后下游流量传感器发生故障。

### 3. `simulation_manager.py` - 核心流程编排器

这是项目中最核心的类，扮演着“导演”的角色，严格按照预设的工作流来调度其他所有组件。

- **初始化 (`__init__`)**:
    - 实例化所有需要的对象：两个渠道模型(A和B)、闸门模型、传感器、数据预处理器、故障检测器、系统辨识器和可视化工具。
    - 这种设计将组件的创建和它们的运行时逻辑分离开来，提高了代码的可读性和可维护性。

- **主循环 (`run_simulation`)**:
    该方法包含一个从`t=0`到仿真结束的时间循环。在**每个时间步**内，它严格按以下顺序执行数据处理管道：
    1.  **检查并应用扰动**: 检查当前时间点是否有预设的扰动事件发生，并应用它。
    2.  **步进“真实世界”**: 调用模型A (`model_a.step()`)，模拟物理世界的真实响应。
    3.  **模拟传感器**: 从模型A的输出中提取真值，并调用`sensor_sim.get_readings()`来生成带噪声的原始读数。
    4.  **数据清洗**: 调用`preprocessor.filter()`对原始读数进行滤波。
    5.  **故障诊断**: 调用`fault_detector.diagnose()`。这是关键决策点，该方法会返回经过甄别的、可靠的数据集以及系统的状态信息。
    6.  **参数辨识**: **仅当**所需数据被诊断为可靠时，才会调用`identifier.run_rls_step()`来更新模型B的参数。
    7.  **孪生同步**: 将辨识出的新参数喂给模型B (`model_b.update_params()`)。
    8.  **步进孪生模型**: 调用`model_b.step()`来获得孪生模型的预测输出。
    9.  **记录与可视化**: 将当前时间步的所有关键信息（真值、孪生预测值、辨识出的参数、故障状态等）传递给`visualizer`进行记录。

### 4. `diagnostics` 模块 - 框架的大脑

这个包是实现数字孪生“同步”和“自适应”能力的核心。

#### 4.1 `data_preprocessor.py`

- **功能**: 接收来自`sensor_simulator`的、包含噪声的原始数据，并对其进行平滑处理。
- **算法**: 当前实现了一个简单的**滑动平均滤波器**。它为每个传感器信号维护一个固定大小的队列（缓冲区）。每当新数据点到达时，它被添加到队列中，同时最旧的数据点被移除。然后，返回队列中所有数据点的平均值。
- **目的**: 减少高频噪声对后续诊断和辨识算法的干扰，提高系统的稳定性。

#### 4.2 `fault_detector.py`

- **功能**: 诊断传感器数据是否可靠，并隔离已知的故障传感器。这是保障整个数字孪生系统可靠性的关键防线。
- **核心设计 (Stateful)**: 此模块是**有状态的**，它内部维护一个`active_faults`集合，用于“记住”哪些传感器已经被判定为故障。这确保了故障一经确认，就会被持续隔离，避免了在每个时间步重复误判。
- **诊断逻辑**:
    1.  **基于冗余的交叉验证**: 这是主要的诊断方法。系统中有两种方式可以获得过闸流量：
        - **理论计算**: 使用`gate_model`和最新的水力参数（`h_up`, `h_down`, `Cq`）计算出理论流量 `q_calc`。
        - **传感器测量**: 直接读取下游流量传感器 `q_gate_down` 的值。
    2.  **故障判据**: 当`q_calc`和`q_gate_down`之间的相对误差超过预设阈值（`fault_threshold`），或当传感器读数卡在一个不合理的低值（如0）而理论计算流量很大时，系统就判定传感器`q_gate_down`发生故障。
    3.  **故障隔离**: 一旦传感器被判定为故障，其ID就被添加到`active_faults`集合中。在后续的每个时间步，`diagnose`方法会首先检查这个集合，并自动从返回的`reliable_data`中剔除所有已知故障传感器的数据。

#### 4.3 `system_identifier.py`

- **功能**: 在确认传感器数据可靠后，利用这些数据来在线估计和更新模型的未知参数。
- **核心算法 (RLS)**: 当前版本实现了**带遗忘因子的递归最小二乘法 (RLS)**。
    - **辨识目标**: 轻量化模型B (`id_channel_model`) 的参数 `theta = [a1, b1]`。该模型可表示为 `h_down(k) = a1*h_down(k-1) + b1*q_up(k-d)`。
    - **工作方式**: RLS算法在每个时间步接收新的输入-输出数据对（即`[h_down(k-1), q_up(k-d)]`和`h_down(k)`），并根据最新的预测误差来微调`theta`的估计值，使其能更好地预测新的输出。
    - **遗忘因子 (`lambda`)**: 小于1的遗忘因子使得算法能够“忘记”旧的数据，从而更关注于最新的系统动态。这使得辨识出的参数能够跟踪系统随时间发生的变化，是实现自适应的关键。
- **可扩展性**: 该模块的设计允许未来轻松集成更复杂的算法，如用于估计模型A参数（如糙率`n`）或未监测入流（`q_lateral`）的**扩展卡尔曼滤波 (EKF)** 或 **无迹卡尔曼滤波 (UKF)**。

### 5. `models` 模块 - 物理世界的数学表达

#### 5.1 `gate_model.py`

- **功能**: 模拟水流通过单孔平板闸门的物理过程。
- **数学公式**:
  ```
  Q_gate = C_q * a * b * sqrt(2 * g * h_up)
  ```
  其中 `C_q` 是流量系数, `a` 是闸门开度, `b` 是闸门宽度, `h_up` 是闸前水深。这是一个简化的堰流公式，但在该框架中，`C_q`可以被在线辨识，从而校正公式的系统误差。
- **作用**: 在仿真中，它有两个用途：
    1. 在故障诊断模块中，用于计算理论流量 `q_calc`，与传感器读数进行比对。
    2. 为模型B提供输入（当模型B用于模拟闸门下游的渠道时）。

#### 5.2 `fvm_channel_model.py` (模型A - "真实世界")

- **功能**: 作为高保真模型，模拟渠道中水流的真实物理行为，为整个系统提供基准真相数据。
- **方法**: 设计上采用**有限体积法 (Finite Volume Method, FVM)** 求解**一维圣维南方程组**。虽然当前版本为了快速搭建框架而使用了占位符逻辑，但其初始化过程是符合物理实际的。
- **关键实现细节**:
    - **初始化**: 为了避免仿真开始时出现虚假的“模型冲击”，该模型的初始流量 `Q` 不是设为0，而是根据初始水深 `h` 和渠道的曼宁糙率、坡度等，通过**曼宁公式**计算出对应的**均匀流流量**。这确保了仿真从一个物理上稳定的状态开始。
    - **`step()` 方法**: 预留了FVM求解器的核心逻辑位置，未来可以被替换为一个完整的、基于如HLL等数值格式的求解器。

#### 5.3 `id_channel_model.py` (模型B - "数字孪生")

- **功能**: 作为轻量化的数字孪生模型，旨在通过不断更新其参数来快速、准确地复现模型A的行为。
- **方法**: 采用了一个离散时间的**积分时滞模型**。其数学形式可以写为：
  ```
  y(k) = a1 * y(k-1) + b1 * u(k-d)
  ```
  - `y(k)`: 当前时刻的输出（如下游水位）。
  - `y(k-1)`: 上一时刻的输出。
  - `u(k-d)`: 经过`d`个时间步延迟的输入（如上游流量）。
  - `a1`, `b1`: 待辨识的模型参数，分别反映了系统的自回归特性和对输入的响应增益。
- **核心方法**:
    - **`step()`**: 根据当前参数和历史输入输出来预测下一时刻的输出。
    - **`update_params()`**: 接收来自`system_identifier`辨识出的新参数 `a1` 和 `b1`，并更新自身模型。这是实现模型自适应和与“真实世界”同步的关键接口。

---
## 第三部分：单一扰动场景演示与验证

本部分将详细介绍如何设置和运行不同的单一扰动场景，以验证框架在特定挑战下的响应能力。

### 场景A: 传感器故障 (Sensor Fault)

这是最关键的诊断场景之一，用于验证系统检测和隔离不可靠数据的能力。

#### 1. 场景描述
仿真开始后，系统首先在正常状态下运行。在此期间，在线辨识算法（RLS）会根据可靠的传感器数据，持续学习并优化模型B的参数，使其输出逐渐逼近模型A。

在仿真进行到第1500秒时，我们**人为地**向下游流量传感器（`q_gate_down`）注入一个“卡死在0”的故障。

#### 2. 预期系统行为
- **故障检测**: 在故障发生后，`FaultDetector`应能迅速识别出“计算流量”（来自闸门公式）与“传感器测量值”（现在为0）之间的巨大差异。
- **故障隔离**: 系统应立即将该传感器标记为“故障”，并将其从“可靠数据集”中排除。此隔离状态应一直持续到仿真结束。
- **系统鲁棒性**: 即使缺少了流量传感器的读数，整个仿真框架也**不应崩溃**。
- **辨识中止**: 由于辨识模型B所需的流量数据已被隔离，RLS参数辨识过程应自动中止，模型B的参数将保持在故障发生前的最后一次更新值。

#### 3. 运行代码
在 `main.py` 中，我们定义了此场景的运行函数：
```python
def run_sensor_fault_scenario():
    # ... 初始化管理器 ...
    sim_manager = SimulationManager(config)

    # 调度扰动事件
    sim_manager.add_perturbation(
        event_time=1500,
        event_type='sensor_fault',
        sensor_id='q_gate_down',
        fault_type='stuck_at_zero'
    )

    sim_manager.run_simulation()
```

#### 4. 结果分析
运行此场景后，我们可以从控制台日志和输出的图片中验证预期的行为：
- **控制台日志**:
  - 在1500秒之前，日志显示 `All systems nominal.`，并且 `INFO: Model B parameters updated...` 消息持续出现。
  - 在1500秒时，日志会打印出 `WARNING: Fault detected in 'q_gate_down'!`。
  - 在1500秒之后，日志会持续显示 `INFO: Sensor 'q_gate_down' is isolated due to a persistent fault.`，并且 `Model B parameters updated` 消息不再出现。
- **输出图表**:
  - `parameter_convergence_param_a1.png` 和 `parameter_convergence_param_b1.png` 会显示参数曲线在1500秒之前不断变化和收敛，而在1500秒之后变成一条水平线，证明了辨识过程已中止。
  - `water_level_comparison.png` 会显示模型B的预测在1500秒之前能很好地跟踪模型A的真值，但在之后由于参数不再更新，可能会出现偏差（取决于系统在故障后是否还有其他动态变化）。

**结论**: 此场景的成功验证，证明了本框架具备了对传感器故障的诊断和隔离能力，是构建可靠数字孪生系统的基础。

### 场景B: 模型参数变化 (糙率变化)

此场景用于验证系统在没有传感器故障，但物理世界本身发生变化时的自适应能力。

#### 1. 场景描述
系统在正常状态下运行1800秒，在此期间模型B的参数趋于稳定。在第1800秒时，我们**修改模型A的曼宁糙率系数`n`**（从0.025变为0.040），这模拟了渠道中水草生长或泥沙淤积导致的物理特性变化。

这种变化是模型B的简化结构无法直接表达的，但它会改变系统的整体水力响应特性（例如，同样的流量下，水位会变得更高）。

#### 2. 预期系统行为
- **无故障诊断**: `FaultDetector`不应报告任何故障，因为所有传感器都在“诚实地”报告它们测量到的、已经变化了的物理值。日志应持续显示`All systems nominal.`。
- **孪生模型失准**: 在`t=1800s`之后，由于模型A的行为已经改变，而模型B的参数还是旧的，因此模型B的预测会开始与模型A的真值产生明显的、持续的偏差。
- **参数自适应**: 为了消除这个新的预测误差，`SystemIdentifier`中的RLS算法会**自动重新开始工作**，调整模型B的参数`a1`和`b1`，试图找到一组新的参数来最好地拟合变化后的系统行为。
- **孪生再同步**: 经过一段时间的自适应调整，模型B的参数会收敛到一个新的稳定状态，其预测输出应能再次紧密地跟踪模型A的输出，实现“再同步”。

#### 3. 运行代码
在 `main.py` 中，我们定义此场景的运行函数：
```python
def run_roughness_change_scenario():
    # ... 初始化管理器 ...
    sim_manager = SimulationManager(config)

    # 调度扰动事件
    sim_manager.add_perturbation(
        event_time=1800,
        event_type='roughness_change',
        new_roughness=0.040 # 模拟水草生长，糙率增大
    )

    sim_manager.run_simulation()
```

#### 4. 结果分析
- **控制台日志**: `All systems nominal.`消息应贯穿始终。`INFO: Model B parameters updated...`消息在整个仿真过程中都应出现，但在1800秒后，参数值应出现显著的波动，并最终收敛到新的值。
- **输出图表**:
  - `parameter_convergence_*.png`: 参数曲线在1800秒处应有明显的转折，开始向新的稳定值收敛。
  - `water_level_comparison.png`: 在1800秒后，应能观察到模型B的预测出现一个短暂的、明显的偏离，然后通过参数的自适应调整，逐渐再次贴合模型A的曲线。

**结论**: 此场景的成功将证明，即使孪生模型的结构比真实世界简单，通过在线辨识与自适应，它也能够有效地“学习”和“补偿”由物理世界内部参数变化带来的影响，从而维持同步。

---
## 第四部分：组合扰动场景

此最终场景旨在测试系统在面对多重、连续的扰动时的综合诊断与适应能力。

### 1. 场景描述
这是一个更加复杂的测试，融合了物理模型变化和传感器故障：
1.  **t = 1000s**: 模型A的渠道糙率`n`发生变化，模拟物理系统的渐变或突变。
2.  **t = 2500s**: 在系统仍在适应前一个变化的过程中，下游流量传感器`q_gate_down`突然发生“卡死在0”的故障。

### 2. 预期系统行为
- **阶段一 (1000s - 2499s)**:
    - 系统应表现出与“场景B”类似的行为。`FaultDetector`保持静默，而`SystemIdentifier`会努力调整模型B的参数，以适应糙率变化带来的新动态。
- **阶段二 (t >= 2500s)**:
    - 系统应表现出与“场景A”类似的行为。`FaultDetector`应立即检测到流量传感器的故障并将其隔离。
    - 从这一刻起，系统辨识过程应完全停止，因为其依赖的关键数据源已经失效。模型B的参数将“冻结”在故障发生前的最后一个状态。

### 3. 运行代码
```python
def run_combined_scenario():
    # ... 初始化管理器 ...
    sim_manager = SimulationManager(config)

    # 调度糙率变化事件
    sim_manager.add_perturbation(
        event_time=1000,
        event_type='roughness_change',
        new_roughness=0.035
    )

    # 调度传感器故障事件
    sim_manager.add_perturbation(
        event_time=2500,
        event_type='sensor_fault',
        sensor_id='q_gate_down',
        fault_type='stuck_at_zero'
    )

    sim_manager.run_simulation()
```

### 4. 结果分析
- **控制台日志**:
    - `0s-999s`: `All systems nominal`，参数辨识正常进行。
    - `1000s-2499s`: 仍然是`All systems nominal`，但参数辨识活动会因为模型失配而变得更加“剧烈”。
    - `t=2500s`: 日志中出现`WARNING: Fault detected...`。
    - `t>2500s`: 日志持续报告传感器被隔离，并且参数不再更新。
- **输出图表**:
    - **参数收敛图**: 参数曲线在`t=1000s`时应出现第一个转折，开始适应新糙率。在`t=2500s`时，曲线应变为一条水平线，表明辨识已停止。
    - **水位对比图**: 在`t=1000s`后，模型B的预测会出现偏差然后逐渐被校正。在`t=2500s`后，由于参数冻结，其预测能力可能会再次下降。

**最终结论**: 这个组合场景的成功运行，完整地展示了本数字孪生框架的核心价值：一个具备分层诊断能力的、鲁棒的、可自适应的仿真系统。它不仅能适应物理世界的变化，还能在感知系统本身出现问题时进行自我诊断和隔离，保证了整体的可靠性。
